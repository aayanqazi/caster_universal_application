import React from "react";

function HelloRender(props,state) {
    return <div>
        <p>{this.state.increment}</p>
        <button onClick={()=>this.increment_Button()}> Click Me </button>
    </div>
}

export default HelloRender;